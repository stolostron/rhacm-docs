[#ts-hcp-bm]
= Troubleshooting hosted clusters on bare metal

When you troubleshoot a hosted cluster on bare metal, start with the top-level `HostedCluster` and `NodePool` resources and then work down the stack until you find the root cause. The following steps can help you discover the root cause of common issues.