:_mod-docs-content-type: PROCEDURE

[#auto-import-capi]
= Enabling automatic import for Cluster API clusters (Technology Preview)

[role="_abstract"]
You can save time by automatically importing clusters that you provision by using the Cluster API.

*Required access:* Cluster administrator

.Prerequisites

* You must prepare for automatic import by following the steps in xref:../cluster_api/auto_import_capi_prep.adoc##prep-auto-import-capi[Preparing your hub cluster for Cluster API clusters (Technology Preview)].

.Procedure

To enable automatic import, create a `ManagedCluster` resource with the same name and namespace as your Cluster API cluster. Complete the following steps:

. Create a `ManagedCluster` resource  by adding the following YAML sample. Add the name of your Cluster API cluster to the `name` parameter:

+
[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: <clusterapi_cluster_name>
spec:
  hubAcceptsClient: true
----

. Run the following command to apply the changes. Replace `<filename>` with your file name:

+
[source,bash]
----
oc apply -f <filename>.yaml
----
