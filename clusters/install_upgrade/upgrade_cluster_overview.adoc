:_mod-docs-content-type: REFERENCE

[#upgrade-clusters-mce-overview]
= Upgrading disconnected clusters by using policies

If you use the {acm-short} hub cluster to manage, upgrade, and install components with the the `MultiClusterHub` operator, you can use the {update-service} and {acm-short} policies to upgrade multiple clusters in a disconnected environment.

{update-service} is a separate operator and operand that monitors available versions for managed clusters and makes them accessible for upgrading in restricted networks.

{update-service} performs the following actions:

* Monitors upgrade availability for disconnected clusters.
* Identifies updates mirrored to your local site by using the graph data file.
* Notifies you of available upgrades via the console.

See the following procedures to prepare and upgrade clusters in a disconnected environment:

* xref:./install_upgrade/prepare_disconnected_registry.adoc#prepare-disconnected-registry[Preparing the disconnected mirror registry to use with {update-service}]
* xref:./install_upgrade/deploy_update_service_operator.adoc#deploy-update-service-operator[Deploying the {update-service} operator]
* xref:./install_upgrade/build_graph_init_update_service.adoc#build-graph-init[Building the graph data init container]
* xref:./install_upgrade/deploy_update_service_instance.adoc#deploy-update-service-instance[Deploying the {update-service} instance]
* xref:./install_upgrade/deploy_policy_change_registry.adoc#deploy-policy-change-registry[Optional: Deploying a policy to change the default image registry]
* xref:./install_upgrade/deploy_policy_change_location.adoc#deploy-policy-change-location[Optional: Deploying a _Catalogsource_ policy to change default location]
* xref:./install_upgrade/deploy_policy_change_parameter.adoc#deploy-policy-change-parameter[Optional: Deploying the _ClusterVersion_ policy to change the upgrade location]
* xref:./install_upgrade/upgrade_clusters_disconnected.adoc#upgrade-clusters-disconnected[Upgrading clusters in a disconnected environment]