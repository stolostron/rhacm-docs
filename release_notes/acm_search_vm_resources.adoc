:_mod-docs-content-type: PROCEDURE

[#search-vm-resources]
= Search results for virtual machine only are displayed

[role="_abstract"]
After you enable fine-grained RBAC and attempt to search resources, only virtual machine resources in the `kubevirt.io` `ClusterRoles` are displayed and not other resources. To workaround this issue, disable the `fine-grained-rbac` feature.

.Procedure

. Edit your `MultiClusterHub` resource by running the following command:

+
[source,bash]
----
oc edit mch -n open-cluster-management multiclusterhub
----

. Edit to change the `configOverrides` specification from `enabled: true` to `enabled: false`. See the following example with the feature disabled:

+
[source,yaml]
----
    - configOverrides: {}
      enabled: false
      name: fine-grained-rbac
----

.Additional resources

* link:https://issues.redhat.com/browse/ACM-26537[ACM-26537]
