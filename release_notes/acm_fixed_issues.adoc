[#errata-acm]
= Fixed issues for {acm-short}

By default, fixed issues for _errata_ releases are automatically applied when released. The details are published here when the release is available. If no release notes are listed, the product does not have an errata release at this time.

*Important:* For reference, Jira links and Jira numbers might be added to the content and used internally. Links that require access might not be available for the user. Learn about the types of link:https:access.redhat.com/security/updates/advisory[errata releases] from Red Hat.

See link:../install/upgrade_hub.adoc#upgrading-by-using-the-operator[Upgrading by using the operator] for more information about upgrades.

*Important:* Cluster lifecycle components and features are within the {mce-short}, which is a software operator that enhances cluster fleet management. Release notes for {mce-short}-specific features are found in at link:../clusters/release_notes/mce_release_notes.adoc#mce-release-notes[Release notes for Cluster lifecycle with {mce-short}].

== Errata 2.14.2

* Delivers updates to one or more product container images.

* Addresses an issue that might cause the console to become unavailable because of a `CrashLoopBackOff` condition by improving performance and increasing probe timeouts on the `console-mce-console` deployment.
link:https://issues.redhat.com/browse/ACM-25039[ACM-25039] 

  * Fixes the `ClusterInstance` admission webhook validation for scale-in operations on multi-node clusters, which incorrectly rejected node removal requests with a `detected unauthorized changes in immutable fields.` message. The webhook matched nodes by array position rather than by identity, so removing a node caused all subsequent nodes to appear modified. The webhook now correctly identifies nodes during scale-in, allowing node removal without false immutable-field violations.
link:https://issues.redhat.com/browse/ACM-25222[ACM-25222] 
//Sharat -- no summary--generated from ai

* Fixes Image-Based Installation failures that were caused by the default `ImageClusterInstall` template not including the `AdditionalNTPSources` field from the `ClusterInstance` specification. Without the field, clusters used the NTP servers from the seed image instead of the production NTP servers, causing installation to fail. The template now correctly renders user-specified NTP sources into the `ImageClusterInstall` manifest.
link:https://issues.redhat.com/browse/ACM-26057[ACM-26057] 
//Sharat -- no summary

* Fixes a problem with disabling the `MulticlusterObservabilityAdd-on` where the Custom Resource Definition status failed to recover, resulting in a persistent `missing` status for the resource. After the fix, the resource correctly transitions when the add-on is disabled or re-enabled.
link:https://issues.redhat.com/browse/ACM-26168[ACM-26168] 

* Adds a delay the platform-type check for observability component, which attempted to determine if a managed cluster was a {ocp-short} cluster too early in the deployment process. The system now ensures that the necessary cluster information is available before determining if the cluster is an {ocp-short} environment, resulting in a more reliable and stable deployment of observability services.
link:https://issues.redhat.com/browse/ACM-27834[ACM-27834] 
//Subbarao - no summary--ai generated

  * Fixes a problem where creating a `ClusterInstance` for an imported or restored cluster incorrectly displayed the `Provisioning in progress` status, even though the cluster was already successfully installed. The `ClusterInstance` now correctly reports a completed provisioning state for these clusters. 
link:https://issues.redhat.com/browse/ACM-29861[ACM-29861] 
//Sharat -- no summary

== Errata 2.14.1

* Delivers updates to one or more product container images.

* Adds a missing link to the operator source code and repository on the OperatorHub installation page. The `annotation.repository` is updated to point to the correct repository URL.
link:https://issues.redhat.com/browse/ACM-24849[ACM-24849]

* Updates the console with a functional *Upgrade* button to start the process with the accurate version for upgrade availability.
link:https://issues.redhat.com/browse/ACM-23943[ACM-23943]

* Fixes pull model GitOps deployments that were stuck in a constant `Refreshing` state in the console to a `Stable` state. The `pull-integration-controller` clears the refresh annotation to prevent applications from getting stuck in a constant `Refreshing` status.
link:https://issues.redhat.com/browse/ACM-23908[ACM-23908]

* Updates validation logic to normalize user-provided Classless Inter-Domain Routing (CIDR) before comparison with the calculated CIDR to prevent a formatting issue in the assisted service validation function. A mismatch between the cluster machine CIDR and the calculated CIDR during IPv6 installation led to failure when users provided uppercase letters, leading zeros, or both.
link:https://issues.redhat.com/browse/ACM-23289[ACM-23289]

* Reduces the excessive `Skipping this secret` logs in the `certificate-policy-controller` resource by logging parsing errors and moving logs about secrets without a certificate.
link:https://issues.redhat.com/browse/ACM-23248[ACM-23248]

* Fixes a race condition where a host that was added to a managed cluster was marked as `installed`, `Done` by {cim} before all of the Certificate Signing Requests were approved. The {cim} now approves all Certificate Signing Requests for hosts that are added to a managed cluster before marking the host as completely `installed` and `Done`.
link:https://issues.redhat.com/browse/ACM-22786[ACM-22786]

* Fixes when a namespace containing a Go template string but no `namespaceSelector` was incorrectly determined to not have a namespace that is specified. The fix ensures that the namespace is correctly identified.
link:https://issues.redhat.com/browse/ACM-22702[ACM-22702]

* Fixes error where the `objectSelector` and `namespaceSelector` were used at the same time, and objects that were discovered by the `objectSelector` in specific namespaces were applied to all namespaces that were returned by the `namespaceSelector`. 
link:https://issues.redhat.com/browse/ACM-22676[ACM-22676]

* Corrects a race condition in the Observability `metric-collector` that occurred when you used multiple workers. This caused some metrics to be incorrectly collected, creating gaps in the metrics data. 
link:https://issues.redhat.com/browse/ACM-24921[ACM-24921]

* Adds the `nmstatectl` binary to the initrd image for the s390x architecture, which was previously missing.
link:https://issues.redhat.com/browse/MGMT-21276[MGMT-21276]

* Corrects a race condition, which caused the static network configuration to not apply on systems with many network interfaces that use full ISO.
link:https://issues.redhat.com/browse/MGMT-21688[MGMT-21688]
