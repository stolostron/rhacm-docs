[#component-resource-requests]
= Resource requests for components

On the hub cluster, the deployment of each component has resource requests that are based on the number of clusters that you manage with {acm-short}. Setting resource requests helps ensure that hub cluster components have sufficient resources to operate effectively.

See the following table for details on the resource requests for each add-on deployment:

.Table list of add-on requests
|===
| Namespace | Deployment | CPU Request | Memory Request 

| `ocm`
| `klusterlet-addon-controller-v2`
| 50m
| 96Mi

| `multicluster-engine` 
| `clusterlifecycle-state-metrics-v2`
| 25m
| 32Mi

| `multicluster-engine`
| `cluster-manager`
| 2m
| 16Mi

| `open-cluster-management-hub` 
| `cluster-manager-addon-manager-controller`
| 2m
| 16Mi

| `open-cluster-management-hub`
| `cluster-manager-placement-controller`
| 2m
| 16Mi

| `open-cluster-management-hub`
| `cluster-manager-registration-controller`
| 2m
| 16Mi

| `open-cluster-management-hub`
| `cluster-manager-registration-webhook`
| 2m
| 16Mi

| `open-cluster-management-hub`
| `cluster-manager-work-webhook`
| 2m
| 16Mi

| `multicluster-engine`
| `cluster-proxy-addon-manager`
| 25m
| 128Mi

| `multicluster-engine`
| `cluster-proxy-addon-user`
| 25m
| 256Mi

| `multicluster-engine`
| `managedcluster-import-controller-v2`
| 50m
| 96Mi

| `multicluster-engine`
| `ocm-controller`
| 100m
| 256Mi

| `multicluster-engine`
| `ocm-proxyserver`
| 100m
| 256Mi

| `multicluster-engine`
| `ocm-webhook`
| 50m
| 128Mi
|===

The resource requests that are listed result from the following environment variables:

* Each cluster belongs to one cluster set.
* Each cluster set has 10 addons and 30 `ManifestWorks` with 50kb installed.
* See the CPU usage calculation: `50m + 0.05m` `*` `CLUSTER_NUMBER`.
* See the memory usage calculation: `1000Mi + 10Mi` `*` `CLUSTER_NUMBER`.

For example, for 50 clusters, the CPU usage is calculated with the following formula: `50m + 0.05m * 50 = 52.5m`.

Memory usage is calculated with the following formula: `1000Mi + 10Mi * 50 = 1500Mi.`

For example, for 100 clusters, the CPU usage is calculated with the following formula: `50m + 0.05m * 100 = 55m`.

Memory usage is is calculated with the following formula: `1000Mi + 10Mi * 100 =  2000Mi`
