:_mod-docs-content-type: CONCEPT

[#gitops-pull-basic]
= Deploying Argo CD with the basic Pull model

[role="_abstract"]
Use the basic Pull model to install the {gitops-short} operator and an independent Argo CD instance on each managed cluster, with application status aggregated on the hub through `ManifestWork`.

*Required access:* Cluster administrator

The basic pull model uses the {gitops} add-on to install the {gitops-short} operator and an Argo CD instance on each managed cluster. Each instance syncs from Git repositories independently. The propagation controller on the hub distributes `Application` resources to managed clusters via `ManifestWork`. The hub shows aggregated overall health and sync status for each application.

*Note:* The basic pull model provides overall Application health and sync status on the hub. If you need full Argo CD UI compatibility with real-time resource trees, logs, and detailed status from managed clusters, use the _advanced pull model_ with the Argo CD Agent instead.

To enable the basic pull model, create a `GitOpsCluster` resource with `gitopsAddon.enabled: true`. The controller creates an `ArgoCD` `Policy` named `<gitopscluster-name>-argocd-policy` that deploys and configures the `ArgoCD` custom resource on managed clusters through the {acm-short} governance framework. The `Policy` is user-owned after creation - you can modify it for RBAC or `ArgoCD` settings. The controller does not overwrite your customizations.

*Required access:* Cluster administrator

*Important:* The basic Pull model is only supported on {ocp-short} managed clusters. For managed clusters that are not {ocp-short}, use the advanced Pull model with the Argo CD Agent.

*Important:* {gitops-short} requires an *{ocp}* subscription. {acm-short} does not include this entitlement. See xref:../gitops/gitops_models.adoc#gitops-models[Understanding the Push and Pull models].

See the following topics:

* xref:../gitops/gitops_pull_basic_enable.adoc#gitops-pull-basic-enable[Enabling the {gitops} add-on for the basic Pull model]
* xref:../gitops/gitops_pull_basic_customize.adoc#gitops-pull-basic-customize[Customizing the Argo CD policy for the basic Pull model]
* xref:../gitops/gitops_pull_basic_deploy.adoc#gitops-pull-basic-deploy[Deploying an application with the basic Pull model]
