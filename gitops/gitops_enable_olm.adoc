:_mod-docs-content-type: PROCEDURE

[#gitops-enable-olm]
= Enabling OLM-based deployment

[role="_abstract"]
If you enable {gitops} add-on without the Argo CD Agent, it is strongly recommended to enable OLM-based deployment for {ocp-short} managed clusters. [NEED TO ADD BENEFIT]

.Prerequisites

- Are there any prereqs?

.Procedure

To enable the OLM-based deployment, complete the following steps:

. On your hub cluster, create a `GitOpsCluster` resource with the `olmSubscription` enabled by adding the following YAML sample:

+
[source,yaml]
----
apiVersion: apps.open-cluster-management.io/v1beta1
kind: GitOpsCluster
metadata:
  name: gitops-clusters-olm
  namespace: openshift-gitops
spec:
  argoServer:
    argoNamespace: openshift-gitops
  placementRef:
    kind: Placement
    apiVersion: cluster.open-cluster-management.io/v1beta1
    name: all-openshift-clusters
    namespace: openshift-gitops
  gitopsAddon:
    enabled: true
    olmSubscription:
      enabled: true
----

. Apply the YAML sample by running the following command:

+
[source,bash]
----
oc apply -f gitopscluster-example.yaml
----

.Additional resources

- xref:../gitops/gitops_addon_without_argocd.adoc#enable-gitops-addon-without-argocd[Enabling {gitops} add-on without the Argo CD Agent]
 
