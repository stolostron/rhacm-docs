[#fine-grain-ui]
= Implementing fine-grained role-based access control with the console

Assign fine-grained virtualization roles to users and groups directly from the console. This allows you to grant specific permissions to manage virtual machines at the cluster or namespace level.

*Required access:* Cluster administrator

== Assigning fine-grained role-based access control in the console

See the following requirements to begin using fine-grained role-based access control:

- Enable the fine-grained role-based access control. See xref:../secure_clusters/fine_grain_rbac_enable.adoc#fine-grain-enable[Enabling fine-grained role-based access control for virtualization] for the procedure.

.Procedure

.Procedure

Complete the following steps to......
//need an introduction here about the procedure, and then we can break down the steps into sections based on the different scopes for role assignments. For example:

* Identities
//steps
* Roles
//steps
* Clusters
//steps
* Cluster sets
//steps



//Draft steps
. From the navigation menu, select the *Fleet management* view. 
. Access the Role assignments:
*Optional:* To access from the *Identities* page, select *User management* > *Identities*, or *Roles*, depending on the permission that you want to assign. click the *Role assignments* tab.
*Optional:* To access from the *Clusters* page, select *Infrastructure* > *Clusters*, then select Cluster list or Cluster set and click the *Role assignments* tab.
. Select the name of the user, group, role, cluster, cluster set for which you want to assign permissions.
. Click *Create role assignment*. 
. When you see the modal in the console, define your scope, role, and/or identity for the role assignment.
+
*Note:* If you select the *Cluster set* scope, you must also define the *Namespace* scope for the set of clusters to which the user or group has permissions.
7. In the *Role* field, select the role you want to assign to the user or group.
8. Click *Create* to save the role assignment.

The new role assignment appears in the *Role assignments* table. The permissions are applied to the selected user or group immediately.