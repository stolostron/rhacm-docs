[#fine-grain-roles-identities]
= Creating role assignments by using identities

Assign fine-grained virtualization roles to users and groups directly from the console. This allows you to grant specific permissions to manage virtual machines at the cluster or namespace level. 

You can assign permissions to a specific user or group across various scopes or roles. Use this method when you need to manage all access levels for a single identity from a central location.

*Required access:* Cluster administrator

.Prerequisites

See the following requirements to begin using fine-grained role-based access control:

* Enable the fine-grained role-based access control. See xref:../secure_clusters/fine_grain_rbac_enable.adoc#fine-grain-enable[Enabling fine-grained role-based access control for virtualization] for the procedure.
* You must be logged in to the {ocp-short} console with cluster administrator or hub administrator permissions.
* The target user or group must already exist in the system. If the identity is not listed, configure your identity provider before you proceed.

.Procedure

. From the navigation menu, expand *User Management* and select *Identities*.

. Select either the *Users* tab, or the *Groups* tab.

. Click the name of the user or group that you want to manage. 

. Select the *Role Assignments* tab.

. Click *Create role assignment*. The configuration window opens with the identity information already populated.

. In the *Scope* section, define the reach of the permissions by selecting one of the following options:
+
Global access:: Applies permissions to all clusters and namespaces in the hub.
Select cluster set:: Applies permissions to a specific group of clusters.
Select clusters:: Applies permissions to individual managed clusters.

. Click *Next*.

. If you did not select *Global access*, select the specific cluster sets or clusters from the list and click *Next*.

. Select the level of granularity for the assignment. See the following options:
+
Cluster role assignment or Cluster set role assignment:: Grants access to all namespaces within the selection.
Project role assignment:: Limits access to specific common projects (namespaces).
//()

. Click *Next*.

. If you selected *Project role assignment*, select the target projects.
+
*Note:* If the required project does not exist, click *Create common project* to define a new one.

. Click *Next*.

. Select the specific role to apply to the identity and click *Next* and review the configuration summary for accuracy. 

. Click *Create* to finalize the role assignment.

The new role assignment appears in the *Role Assignments* table for the selected identity. The permissions take effect the next time the user logs in or refreshes their session.