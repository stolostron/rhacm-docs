:_mod-docs-content-type: PROCEDURE

[#backup-virt-define-configmap]
= Defining policies with _ConfigMap_ resources

[role="_abstract"]
The `acm-dr-virt-config` config map defines the {oadp} configuration and other settings that are related to the backup or restore operation. You can use the name of this resource as the value for the `acm-virt-config` label, which is set on the managed cluster where the policies are placed. You can also create a new `ConfigMap` resource that uses the `acm-dr-virt-config` as a sample, then use your new `ConfigMap` resource to place the virtualization policies on the cluster.

.Procedure

. Use the `acm-dr-virt-config` or create a new `ConfigMap` resource on the hub cluster in the `open-cluster-management-backup` namespace by using the `acm-dr-virt-config-new` `ConfigMap` as an example. 
.. Update the `dpa_spec` section to match the storage location where you want to store the backups that are created by {oadp}.
.. *Optional:* Update other properties that are available with the `acm-dr-virt-config-new` `ConfigMap`, such as {oadp} version or channel.
. Add the `acm-virt-config=acm-dr-virt-config-new` label to the `ManagedCluster` resource. The value of the `acm-virt-config` label is the name of the config map that you created. For an example of a `ManagedCluster` resource with a virtualization backup label set to use `acm-dr-virt-config-new`, see the following YAML sample:

+
[source,yaml]
----
apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
 name: managed-cluster-name
 labels:
   acm-virt-config: acm-dr-virt-config-new
----

.Next steps

* See xref:../backup_virt_configmaps_ref.adoc#backup-virt-configmap-ref[Virtualization policy _ConfigMap_ overview] for ConfigMap properties, schedule configuration, and restore ConfigMap examples.
