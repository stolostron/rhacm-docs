[#edge-mgr-images]
= Operating system images for the {rhem}

Image-based operating systems allow the operating system and the configuration and applications to be versioned, deployed, and updated as a single unit.

Using an image-based operating system reduces operational risks with the following capability:

* Minimize potential drift between what is tested and what is deployed to a large number of devices.
* Minimize the risk of failed updates that require expensive maintenance or replacement through transactional updates and rollbacks.

The {rhem} focuses on image-based Linux operating systems that run bootable container image (`bootc`). For more information, see https://bootc-dev.github.io/bootc/[bootc].

*Important:* The `bootc` tool does not update package-based operating systems.

See the following description for how to build images:

* Choose a base `bootc` operating system image, such as a Fedora, CentOS, or RHEL image.
* Create a container file that layers the following items onto the base `bootc` image:
    ** The {rhem} agent and configuration.
    ** Optional: Any drivers specific to your target deployment environment.
    ** Optional: Host configuration, for example certificate authority bundles, and application workloads that are common to all deployments.
* Build, publish, and sign a `bootc` operating system image using `podman` and `skopeo`.
* Create an operating system disk image by using `bootc-image-builder`.
* Build, publish, and sign an operating system disk image using `skopeo`.
 
*Note:* The operating system disk image contains partitions, volumes, the file system, and the initial `bootc` image. The operating system disk image only needs to be created once, during provisioning.

For subsequent device updates, only the `bootc` operating system image is required, which contains the files in the file system.

See the following image building topics:

* xref:../edge_manager/edge_mgr_build_images.adoc#edge-mgr-build[Building a _bootc_ operating system image for the {rhem}]
* xref:../edge_manager/edge_mgr_images_consider.adoc#build-images-consider[Special considerations for building images]
* xref:../edge_manager/edge_mgr_build_images_platform.adoc#edge-mgr-build-platforms[Building for specific target platforms]
//this link was missing
//Operating system images for using with the {rhem}--this title is confusing???
//if we are creating steps, like these were previously, then it goes in a TASK with a GERUND in the title; this is not designed appropriately as a task. This section will be either a bulleted list or a separate file with a procedure. This file should have been an INTRO file (concept) to contain all image building files, but no procedures. This can stay as it is, but I recommend moving it to it's own file. Listen, we have been clear that we need to set up procedures with gerunds and steps...this will be mandated in DITA and it is the principal that I have tried to keep with ACM, but for some reason we go sideways lately. DITA will force this, so we may as well stick to the standards now, since we have to convert: Procedures go in tasks with gerunds as titles, concept files do not contain procedures. --bcs 7/25