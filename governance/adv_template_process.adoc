:_mod-docs-content-type: CONCEPT

[#adv-template-processing]
= Advanced template processing

[role="_abstract"]
Managed cluster and hub cluster templates reduce the need to create separate policies for each target cluster or hard code configuration values in policy definitions. For security, resource-specific and generic `lookup` functions in hub cluster templates are restricted to the policy namespace on the hub cluster.

*Important:* Using hub cluster templates to propagate secrets or other sensitive data exposes the data in the managed cluster namespace on the hub cluster, and on the managed clusters where that policy is distributed. Template content expands within the policy, and policies are not encrypted by the {ocp-short} etcd encryption support. To address this, use `fromSecret` or `copySecretData` to automatically encrypt values from the secret, or use `protect` to encrypt other values.

Special annotation for reprocessing::

Hub cluster templates are resolved to the data in the referenced resources during policy creation, or when the referenced resources are updated.

+
If you need to manually initiate an update, use the `policy.open-cluster-management.io/trigger-update` annotation to indicate changes for the data referenced by the templates. Any change to the special annotation value automatically initiates template processing. Additionally, the latest contents of the referenced resource are read and updated in the policy definition that is propagated for processing on managed clusters. A way to use this annotation is to increment the value by one each time.

Template processing bypass methods::

By default, {acm-short} processes all Go templates. To bypass template processing for a specific template, change `{{ template content }}` to `{{ pass:[`{{ template content }}`] }}`. Then, the template returns a raw string to be processed by a subsequent templating engine.

+
To completely bypass the template resolution for a configuration policy, add the `policy.open-cluster-management.io/disable-templates: "true"` annotation to the relevant `ConfigurationPolicy` resource within the `Policy` resource.

.Additional resources

* xref:../governance/template_functions.adoc#template-functions[Template functions reference]
* xref:../governance/template_support_intro.adoc#template-processing[Use template processing in policies]
* xref:../governance/manage_policies.adoc[Policy CLI]
* xref:../governance/config_policy_ctrl.adoc#kubernetes-config-policy-controller[Configuration policy controller]
* link:https://docs.redhat.com/en/documentation/openshift_container_platform/4.20/html/backup_and_restore/control-plane-backup-and-restore#backing-up-etcd-data_backup-etcd[Backing up etcd data]
